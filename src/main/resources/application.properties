# ============================================================
# 🧩 CONFIGURACIÓN GENERAL DEL GATEWAY
# ============================================================
server.port=8082
spring.application.name=uco-challenge-apigateway

# ============================================================
# 🌐 RUTAS DEL GATEWAY (EJEMPLO USERS-SERVICE)
# ============================================================
spring.cloud.gateway.routes[0].id=users-service
spring.cloud.gateway.routes[0].uri=http://localhost:8083
spring.cloud.gateway.routes[0].predicates[0]=Path=/users/**
# Quita el prefijo /users antes de enviar la request al backend
spring.cloud.gateway.routes[0].filters[0]=StripPrefix=1

# ============================================================
# 🔒 AUTH0 / RESOURCE SERVER
# ============================================================
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://<AUTH0_DOMAIN>/ # TODO completar valor
auth0.audience=<API_AUDIENCE> # TODO completar valor

# ============================================================
# 🌍 CORS CONFIG (para tu SecurityConfig y global gateway)
# ============================================================
# Lista de orígenes permitidos para el frontend (separados por coma)
web.cors.allowed-origins=<FRONTEND_ORIGINS> # TODO completar valor

# --- CORS GLOBAL PARA SPRING CLOUD GATEWAY ---
spring.cloud.gateway.globalcors.corsConfigurations.[/**].allowedOrigins=<FRONTEND_ORIGINS> # TODO completar valor
spring.cloud.gateway.globalcors.corsConfigurations.[/**].allowedMethods=GET,POST,PATCH,PUT,DELETE,OPTIONS,HEAD
spring.cloud.gateway.globalcors.corsConfigurations.[/**].allowedHeaders=Authorization,Content-Type,X-Requested-With,Accept,Origin
spring.cloud.gateway.globalcors.corsConfigurations.[/**].exposedHeaders=Authorization,X-Get-Header
spring.cloud.gateway.globalcors.corsConfigurations.[/**].allowCredentials=true
spring.cloud.gateway.globalcors.add-to-simple-url-handler-mapping=true

# ============================================================
# ⚙️ ACTUATOR (opcional para health checks)
# ============================================================
management.endpoints.web.exposure.include=health,info

# ============================================================
# 🔐 FIRMA OPCIONAL HACIA MICROSERVICIOS
# ============================================================
gateway.hmac.signature=<GATEWAY_HMAC_SIGNATURE> # TODO completar valor (dejar vacío si no aplica)

# ============================================================
# 🧾 LOGGING (útil mientras depuras seguridad / CORS)
# ============================================================
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.cloud.gateway=DEBUG
logging.level.reactor.netty.http.client=INFO
logging.level.org.springframework.web.cors=DEBUG
